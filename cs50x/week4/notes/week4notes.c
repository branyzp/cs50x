#include <stdio.h>

int main (void)
{
    // * declaring variable n with value 50
    // int n = 50;

    // * declaring pointer p with * syntax to indicate and store address
    // int *p = &n;
    // * giving *p the value of the address of n, hence giving it the ampersand n
    // * when you do a * and the name of a pointer without specifying a type, it means 'Go There'

    // * you can also write it like this:
    // int *p = &n;
    // * meaning, the whitespace does not matter, but by convention we will write it with the star attached to the name of the pointer

    // * pointers take up more space (8 bytes) these days

    // char *s = "HI!";

    // printf("%c\n", *s);
    // printf("%c\n", *(s+1));
    // printf("%c\n", *(s+2));
    

}
